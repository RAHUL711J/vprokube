apiVersion: v1
kind: Service
metadata:
  name: vproapp-service #Fetching the name from application.properties file, as this name should be resolved to an IP addr.
spec:
  ports:
    - targetPort: 8080
      port: 8080
      protocol: TCP
  selector:
    app: vproapp
  type: ClusterIP
