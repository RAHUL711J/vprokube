apiVersion: v1
kind: Service
metadata:
  name: vprocache01 #Fetching the name from application.properties file, as this name should be resolved to an IP addr.
spec:
  ports:
    - targetPort: vpromc-port
      port: 11211
      protocol: TCP
  selector:
    app: vpromc
  type: ClusterIP
